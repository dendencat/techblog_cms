DEBUG=False
SECRET_KEY=your-secret-key-here
ALLOWED_HOSTS=.localhost,127.0.0.1

# Database (choose one of the following approaches)
# 1) DATABASE_URL (recommended for production; supports URLエンコード済みパスワード)
#    例: postgres://user:pass%40word%21@db:5432/appdb
DATABASE_URL=postgres://user:password@db:5432/appdb

# 2) 個別指定（ローカルやCI向け）。特殊文字を含んでもOK（URLエンコード不要）。
POSTGRES_USER=your-db-user
POSTGRES_PASSWORD=your-db-password
POSTGRES_DB=your-db-name

# 3) アプリ専用の資格情報（任意）。指定した場合、DB初期化時にユーザーを作成/更新します。
# APP_DB_USER=app-user
# APP_DB_PASSWORD=app-strong-password
# APP_DB_NAME=appdb

REDIS_PASSWORD=your-redis-password
REDIS_URL=redis://:your-redis-password@redis:6379/1

# OAuth Settings
GOOGLE_CLIENT_ID=your-google-client-id
GOOGLE_CLIENT_SECRET=your-google-client-secret
GITHUB_CLIENT_ID=your-github-client-id
GITHUB_CLIENT_SECRET=your-github-client-secret

# Security Settings
CSRF_TRUSTED_ORIGINS=https://yourdomain.com
SECURE_SSL_REDIRECT=True
SESSION_COOKIE_SECURE=True
CSRF_COOKIE_SECURE=True

# Email Configuration (Production)
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_USE_TLS=True
EMAIL_HOST_USER=your-email@gmail.com
EMAIL_HOST_PASSWORD=your-app-password
DEFAULT_FROM_EMAIL=noreply@yourdomain.com
ADMIN_EMAIL=admin@yourdomain.com

# Domain Configuration
DOMAIN=yourdomain.com

# Sentry Error Tracking (Optional)
# SENTRY_DSN=https://your-sentry-dsn@sentry.io/project-id
